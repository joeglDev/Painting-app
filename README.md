# Painting app

## Known issues

- `Branch opacity` - Overlay features such as changing brush alpha levels result in dotted lines as brush overlays itself in a stroke many times. Branch `opacity` resolves this but results in a bug which causes a change in brush settings to affect all drawn elements on screen.
