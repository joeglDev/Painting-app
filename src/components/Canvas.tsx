import { styled } from "styled-components";

const CanvasWrapper = styled.section`
flex: 80;
padding: 1rem;
`;

export const Canvas = () => {
    return (
        <CanvasWrapper>
            <h2>canvas</h2>
        </CanvasWrapper>
    ) 
}